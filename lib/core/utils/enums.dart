enum Currency {
  egp,
  usd,
}

enum AccountTypes {
  main,
  saving,
  emergency,
  investment,
  retirement,
  cash,
  wallet,
}

enum CardType {
  debit,
  credit,
  prepaid,
}

enum CardCompany {
  visa,
  mastercard,
  meeza,
}

enum ExpensesType {
  income,
  expenses,
  transfer,
}
